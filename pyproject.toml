[tool.ruff]
line-length = 80
select = [
  "E",
  "F",
  "W",
  "Q",
  "U",
  "I",
]
target-version = "py37"
extend-exclude = ["bundled/libs", "src/test/python_tests/test_data"]

[tool.black]
line-length = 80

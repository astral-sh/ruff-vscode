# Bump the extension and bundled Ruff and ruff-lsp versions and create a pull request.
name: Prepare release

on:
  workflow_dispatch:
    inputs:
      version:
        description: Exact extension version to release, without a leading v
        required: true
        type: string
      ruff-version:
        description: Bundled Ruff version (defaults to the latest version on PyPI)
        required: false
        type: string
      ruff-lsp-version:
        description: Bundled ruff-lsp version (defaults to the latest version on PyPI)
        required: false
        type: string

permissions: {}

jobs:
  prepare:
    name: Prepare release
    if: github.repository == 'astral-sh/ruff-vscode' && github.ref_name == github.event.repository.default_branch
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          python-version: "3.13"

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24.19.0"
          package-manager-cache: false

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Bump version
        shell: bash
        run: |
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Invalid extension version: ${VERSION}"
            exit 1
          fi
          if git rev-parse --verify --quiet "refs/tags/${VERSION}" >/dev/null; then
            echo "::error::Tag ${VERSION} already exists"
            exit 1
          fi

          args=(--new-version "$VERSION")
          if [[ -n "$RUFF_VERSION" ]]; then
            args+=(--new-ruff "$RUFF_VERSION")
          fi
          if [[ -n "$RUFF_LSP_VERSION" ]]; then
            args+=(--new-ruff-lsp "$RUFF_LSP_VERSION")
          fi
          uv run --locked scripts/release.py "${args[@]}"

          package_version="$(node -p 'require("./package.json").version')"
          if [[ "$package_version" != "$VERSION" ]]; then
            echo "::error::Requested version ${VERSION} was normalized to ${package_version}"
            exit 1
          fi
        env:
          VERSION: ${{ inputs.version }}
          RUFF_VERSION: ${{ inputs.ruff-version }}
          RUFF_LSP_VERSION: ${{ inputs.ruff-lsp-version }}

      - name: Create or update pull request
        run: |
          branch="release/${VERSION}"
          git switch -c "$branch"
          git add package.json package-lock.json pyproject.toml uv.lock requirements.txt README.md CHANGELOG.md
          git commit -m "Release ${VERSION}"
          git push --force "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" "HEAD:refs/heads/${branch}"

          pull_request="$(
            gh pr list \
              --base "$DEFAULT_BRANCH" \
              --head "$branch" \
              --state open \
              --json url \
              --jq '.[0].url'
          )"
          if [ -n "$pull_request" ]; then
            printf '%s\n' "$pull_request"
          else
            gh pr create \
              --base "$DEFAULT_BRANCH" \
              --head "$branch" \
              --fill-first
          fi
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ inputs.version }}
